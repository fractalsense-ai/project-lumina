{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "lumina://standards/admin-command-schemas/trigger-daemon-task.json",
  "schema_version": "1.0.0",
  "last_updated": "2026-03-31",
  "title": "trigger_daemon_task",
  "description": "Manually trigger a daemon task run, optionally scoped to a domain and specific tasks.",
  "type": "object",
  "required": [
    "operation",
    "params"
  ],
  "additionalProperties": false,
  "properties": {
    "operation": {
      "const": "trigger_daemon_task"
    },
    "target": {
      "type": "string"
    },
    "params": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "domain_id": {
          "type": "string"
        },
        "tasks": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Specific task names to trigger. If omitted, all registered tasks run."
        }
      }
    }
  }
}
