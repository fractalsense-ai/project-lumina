{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "lumina://standards/admin-command-schemas/list-domain-rbac-roles.json",
  "schema_version": "1.0.0",
  "last_updated": "2026-03-31",
  "title": "list-domain-rbac-roles",
  "description": "List domain-specific roles defined in a domain's physics configuration.",
  "type": "object",
  "properties": {
    "operation": { "const": "list_domain_rbac_roles" },
    "target": { "type": "string" },
    "params": {
      "type": "object",
      "properties": {
        "domain_id": { "type": "string", "description": "Target domain identifier" }
      },
      "required": ["domain_id"]
    }
  },
  "required": ["operation", "params"]
}
