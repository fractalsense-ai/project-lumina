{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "lumina://standards/admin-command-schemas/get-domain-module-manifest.json",
  "schema_version": "1.0.0",
  "last_updated": "2026-03-31",
  "title": "get-domain-module-manifest",
  "description": "Retrieve a domain's full module manifest including module IDs, labels, and versions.",
  "type": "object",
  "properties": {
    "operation": { "const": "get_domain_module_manifest" },
    "target": { "type": "string" },
    "params": {
      "type": "object",
      "properties": {
        "domain_id": { "type": "string", "description": "Target domain identifier" }
      },
      "required": ["domain_id"]
    }
  },
  "required": ["operation", "params"]
}
